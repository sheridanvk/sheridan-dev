{
  "name": "sheridan-dev",
  "version": "1.0.0",
  "description": "Sheridan's blog",
  "main": "index.js",
  "scripts": {
    "dev": "npx @11ty/eleventy --serve",
    "debug": "DEBUG=Eleventy* npx @11ty/eleventy",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@11ty/eleventy": "^0.12.1",
    "node-fetch":"^2.0.0"
  }
}
